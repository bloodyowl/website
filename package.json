{
  "name": "bloodyowl",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist",
    "server": "pages start src/App.bs.js 3000",
    "prerender": "pages build src/App.bs.js",
    "release": "rm -fr dist && yarn build && yarn prerender && yarn deploy",
    "format": "find ./src -iname '*.re' | xargs bsrefmt --in-place && find ./src -iname '*.rei' | xargs bsrefmt -i true --in-place"
  },
  "keywords": [
    "BuckleScript"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bs-css-emotion": "^2.1.0",
    "gh-pages": "^2.0.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "reason-react": "^0.9.1",
    "rescript-pages": "0.1.38"
  },
  "devDependencies": {
    "bs-platform": "^8.2.0"
  }
}
